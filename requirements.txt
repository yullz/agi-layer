# agi-layer runs with NO dependencies on the offline echo backend + hashing
# embeddings. Install only what you want to switch on. Prefer `pip install -e .`
# with extras (see pyproject.toml); this file is the flat equivalent.

# --- core (always) ---
pyyaml>=6
tzdata                  # timezone database for zoneinfo (Windows ships none)

# --- turn on real models (pick what you use) ---
# claude-agent-sdk      # Claude on your Pro/Max subscription (the default backend)
# litellm               # Claude/GPT/Gemini via an API key (overflow / alternative)
# Local models use Ollama's HTTP API directly (no Python client needed).

# --- turn on real embeddings + reranking ---
# sentence-transformers # local embeddings (all-MiniLM) + cross-encoder reranker

# --- optional hybrid semantic backend (native is the default) ---
# mem0ai
# chromadb

# --- optional: prompt/routing optimization, scheduler, interfaces ---
# dspy-ai               # GEPA optimizer
# apscheduler           # cron-accurate consolidation (stdlib timer fallback otherwise)
# fastapi               # HTTP API  (AGI_INTERFACE=api)
# uvicorn
# mcp                   # MCP bridge (AGI_INTERFACE=mcp)
