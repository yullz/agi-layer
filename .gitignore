# Your actual memory lives here — never commit it
data/
*.db
*.sqlite*
vectors/
graph/

# Python
__pycache__/
*.pyc
.env
.venv/
venv/
*.egg-info/
.DS_Store

# Tooling / build caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
build/
# Python packaging output — repo root only, so ui/dist stays shipped
/dist/
*.egg
.coverage
htmlcov/

# Memory artifacts (normally under data/, but ignore anywhere in case relocated)
skills/
versions/
*.jsonl
*.consolidation.json
